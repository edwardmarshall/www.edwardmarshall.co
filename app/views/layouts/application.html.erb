<!DOCTYPE html>
<html>
<head>
	<title><%= content_for?(:title) ? content_for(:title) : "Edward Marshall" %></title>

	<!-- META DATA -->
	<!-- OPEN GRAPH META HERE -->

	<%= csrf_meta_tags %>

	<!-- STYLESHEETS -->
	<%= stylesheet_link_tag "application", :media => "all" %>

	<!-- SCRIPTS -->
	<%= javascript_include_tag "application" %>

	<!-- PAGE SCRIPTS -->
	<%= yield :headscripts %>
</head>
<body class="<%= "#{controller.controller_name} #{@body_class}".strip %>">
	
	<%= yield %>

</body>
</html>
